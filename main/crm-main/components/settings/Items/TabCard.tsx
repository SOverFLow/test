import { Grid, Typography, styled } from "@mui/material";

export const TabCard = styled(Grid)(() => ({
  
    border: "1px solid black",
    width: "100%",
    p: 2,
    padding: "1rem",
    borderRadius: "12px",
    borderColor: "#DEDEDE",
}));
